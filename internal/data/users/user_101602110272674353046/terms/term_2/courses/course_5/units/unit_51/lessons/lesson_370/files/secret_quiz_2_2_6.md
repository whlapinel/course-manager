# Unit 2 Quiz: Python 2
---

## 1. Binary Numbers and Bitwise Operations  

**Q1:** Which operator is used for the bitwise AND operation in Python?  
- A) `&`  
- B) `&&`  
- C) `and`  
- D) `|`

**Q2 (Short Answer):** Convert the decimal number **10** to its binary representation.

---

## 2. Creating and Importing Modules  

**Q3 (Short Answer):** In Python, how would you create your own module? Briefly describe the steps.

**Q4 (Short Code Example):** Write a small code snippet to import a custom module named `my_module` and call a function `say_hello()` from it.

---

## 3. Using the `datetime` Module  

**Q5 (Short Answer):** Which function from the `datetime` module can you use to get the current date and time in Python? (Give the function name and a short code example.)

---

## 4. Easy Recursion  

**Q6 (Short Code Example):** Write a **simple recursive function** that calculates the factorial of a given positive integer `n`.  

*(Hint: factorial of 0 is 1, and factorial of n = n * factorial(n-1))*

---

## 5. Nested Loops  

**Q7 (Short Answer & Example):** What is a nested loop in Python? Provide a small code snippet of a nested loop that prints the following pattern (3 rows, each containing 3 asterisks):  
```
***
***
***
```

---

## 6. Complex Conditionals  

**Q8 (Rewrite the Condition):** You have this code:
```python
if x > 0 and x < 10:
    print("Valid")
else:
    print("Invalid")
```
Rewrite it using a single combined comparison operator (i.e., chain the comparisons).

---

## 7. If-Else Statements, While Loops, and For Loops  

**Q9 (Short Code Example):** Write a simple **if-else** statement that checks if a number `num` is even or odd and prints `"Even"` or `"Odd"`.  

**Q10 (Short Code Example):** Write a **while** loop that prints numbers from 1 to 5.  

**Q11 (Short Code Example):** Write a **for** loop that iterates over a list `["apple", "banana", "cherry"]` and prints each fruit.

---

## 8. Functions  

**Q12 (Short Code Example):** Define a function named `greet(name)` that takes a single parameter `name` and prints `"Hello, <name>!"`.

---

## 9. Basic Data Structures (Dictionaries, Lists, Tuples)  

**Q13 (Short Answer):** Name **one key difference** between each of the following in Python:  
- List  
- Tuple  
- Dictionary  

**Q14 (Short Code Example):** Create a dictionary named `ages` with at least three key-value pairs (e.g., names as keys, ages as values). Then print the age of one person using the person’s name as the key.

**Q15 (True/False):** Tuples in Python are **immutable**, meaning their contents cannot be changed after creation.
